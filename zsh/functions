export NVM_DIR=~/.nvm

# 'p name' = 'cd ~/dev/name & nvim .'
# 'p name src/index.ts' = 'cd ~/dev/name & nvim ./src/index.ts'
function dev() { 
	cd ~/dev/$1 
	nvim $2
}

function config() {
	cd ~/.config
	nvim $1
}
